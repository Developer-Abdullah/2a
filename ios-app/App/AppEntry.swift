import SwiftUI
@main
struct PlatformStoreApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Platform iOS App Ready!")
                .font(.largeTitle)
                .foregroundColor(.blue)
        }
    }
}
